const find = {
    bindings: {},
    controller: function () {
        let ctrl = this;

        ctrl.$onInit = function () {
        };

    },
    template: `
    `
};

angular.module('RowdyMermaid-site.find')
    .component('find', find);

find.$inject = [];
